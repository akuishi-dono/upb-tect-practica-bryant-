# Simple Modular Calculator

A simple calculator implemented in JavaScript using ES6 modules.

## Usage

Run the calculator:

```bash
npm start
```

The calculator will prompt for two numbers and an operation.